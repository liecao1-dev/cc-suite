interface:
  display_name: "派遣给 Claude"
  short_description: "每次手选模型配置，再把任务交给 Claude Code"
  default_prompt: "Use $claude to send this task to Claude Code after I choose the configuration."
dependencies:
  tools:
    - type: "mcp"
      value: "claude-code"
      description: "The cc-suite managed claude-octopus MCP server."
policy:
  allow_implicit_invocation: false
