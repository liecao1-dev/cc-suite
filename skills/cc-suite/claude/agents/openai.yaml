interface:
  display_name: "Claude 派遣"
  short_description: "每次先手动选择 Claude 模型配置，再输入或派遣本次任务"
  default_prompt: "Use $claude to let me choose a Claude configuration first, then ask for my task."
dependencies:
  tools:
    - type: "mcp"
      value: "claude-code"
      description: "The cc-suite managed claude-octopus MCP server."
policy:
  allow_implicit_invocation: false
